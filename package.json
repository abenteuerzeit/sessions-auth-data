{
  "name": "sessions-auth",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run open && nodemon app.js",
    "open": "concurrently \"http-server -a localhost -p 4001\" \"open http://localhost:4001/login\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "http-server": "^14.1.1"
  }
}
